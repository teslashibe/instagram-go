# Instagram Direct private-API capture

Captured at: `2026-08-01T19:00:00Z`  
Result: **complete for inbox pagination, thread retrieval, thread creation, and text broadcast**.

> Normalized from an explicitly approved burner/self capture. Cookie, Authorization, CSRF, recipient, viewer, thread, item, cursor, device, client-context, message-text, profile, and raw response values were discarded during secret review.

## Inbox pagination

- Request: `GET https://i.instagram.com/api/v1/direct_v2/inbox/`
- Request fields: `limit`, `persistentBadging`, `thread_message_limit`, `visual_message_return_type`; continuation adds `cursor`
- Header names: `Accept`, `Accept-Language`, `Cookie`, `User-Agent`, `X-CSRFToken`, `X-IG-App-ID`, `X-IG-Capabilities`, `X-IG-Connection-Type`
- Pagination fields: `$.inbox.has_older`, `$.inbox.oldest_cursor`
- Response field paths: `$.inbox`, `$.inbox.threads[]`, `$.inbox.threads[].thread_id`, `$.inbox.threads[].thread_title`, `$.inbox.threads[].users[]`, `$.inbox.threads[].items[]`, `$.inbox.threads[].last_activity_at`, `$.inbox.threads[].is_group`, `$.inbox.threads[].is_pending`, `$.inbox.threads[].muted`, `$.inbox.threads[].read_state`, `$.status`

## Thread retrieval

- Request: `GET https://i.instagram.com/api/v1/direct_v2/threads/{thread_id}/`
- Ownership proof: the redacted path ID matched a thread returned by the same authenticated inbox capture; the value was then discarded
- Request fields: `limit`, `visual_message_return_type`; continuation adds `cursor`
- Pagination fields: `$.thread.has_older`, `$.thread.oldest_cursor`
- Response field paths: `$.thread`, `$.thread.thread_id`, `$.thread.items[]`, `$.thread.items[].item_id`, `$.thread.items[].user_id`, `$.thread.items[].item_type`, `$.thread.items[].text`, `$.thread.items[].timestamp`, `$.thread.items[].client_context`, `$.status`

## Thread creation

- Request: `POST https://i.instagram.com/api/v1/direct_v2/create_group_thread/`
- Request fields: `_uuid`, `recipient_users`
- Approval proof: `recipient_users` contained exactly one ID and matched both operator approval values; all three values were then discarded
- Response field paths: `$.thread`, `$.thread.thread_id`, `$.thread.users[]`, `$.status`

## Text broadcast

- Request: `POST https://i.instagram.com/api/v1/direct_v2/threads/broadcast/text/`
- Request fields: `_uuid`, `action`, `client_context`, `mutation_token`, `offline_threading_id`, `text`, `thread_ids`
- Mutation proof: HTTP was successful with `status=ok`; text was non-empty; client context equaled mutation token; thread IDs contained only the created thread; `payload.item_id` was non-empty; all values were then discarded
- Response field paths: `$.payload`, `$.payload.item_id`, `$.payload.client_context`, `$.status`

## Boundary

This capture does not verify attachments, reactions, vanish mode, or group
administration. Those operations remain unsupported.
